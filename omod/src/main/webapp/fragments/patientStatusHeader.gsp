<% if(shouldBeDisplayed) { %>
    <div class="identifiers patientLocation" style="float:right">
        <em>${ attributeLabel }</em>
        <span>${ui.message(attributeValue)}</span>
        <br>
    </div>
<% } %>
